doctype html
html(lang='en' domain=metadata.domain)
  head
    include ../meta.pug
    title #{title || metadata.title}
  body(class='text-gray-900 bg-gray-100 dark:text-gray-100 dark:bg-gray-900') !{content}
